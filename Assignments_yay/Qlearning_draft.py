import numpy as np
import time
import random


def qlearning(env, gamma, theta: ####)    
    episode_count = 0
    V, policy = env.initialize()
    # ! initialize Q table
    while not done / loop for each episode / while not_converged:
        while state != terminal_state (# loop for each epsidoe):
            episode_count += 1
            # ! initialize S
            # ! loop for each step in episode


    return V, policy, loop_counter, elapsed_time



def choose_action(policy_ = e-greedy):


    return action
